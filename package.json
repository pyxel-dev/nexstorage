{
  "name": "nextstorage",
  "version": "1.0.0",
  "description": "A tiny storage for web app",
  "main": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "prepare": "npm run test && npm run build"
  },
  "author": "Kevin Py",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^13.11.0"
  },
  "devDependencies": {
    "@types/jest": "^25.2.1",
    "jest": "^25.2.7",
    "ts-jest": "^25.3.1",
    "typescript": "^3.8.3"
  }
}
