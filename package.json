{
  "name": "nextstorage",
  "version": "0.0.1",
  "description": "A tiny storage for web app",
  "main": "./src/index.ts",
  "types": "./dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "tsc",
    "test": "jest --coverage",
    "prepare": "npm run test && npm run build"
  },
  "author": "Kevin Py",
  "license": "MIT",
  "dependencies": {
    "@types/node": "^12.7.5"
  },
  "devDependencies": {
    "@types/jest": "^24.0.18",
    "jest": "^24.9.0",
    "ts-jest": "^24.1.0",
    "typescript": "^3.6.3"
  }
}
